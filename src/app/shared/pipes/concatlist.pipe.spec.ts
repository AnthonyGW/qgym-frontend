import { ConcatlistPipe } from './concatlist.pipe';

describe('ConcatlistPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatlistPipe();
    expect(pipe).toBeTruthy();
  });
});
